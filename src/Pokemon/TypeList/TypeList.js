import React, { useState } from "react";

const TypeList = ({ type }) => {
  return (
    <React.Fragment>
      <div class="type">{type}</div>
    </React.Fragment>
  );
};

export default TypeList;
